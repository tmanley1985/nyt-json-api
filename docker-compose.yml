services:
    web:
        build:
            context: ./
            dockerfile: web.Dockerfile
        volumes:
            - ./:/var/www
        ports:
            - "8080:80"
        links:
            - app
    app:
        build:
            context: ./
            dockerfile: app.Dockerfile
        volumes:
            - ./:/var/www
